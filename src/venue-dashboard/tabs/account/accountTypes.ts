export type DayHours = {
  day: string;
  shortDay: string;
  openTime: string;
  closeTime: string;
  isClosed: boolean;
};

export type LiveyDropdownOption = {
  label: string;
  value: string;
};

export type OpeningHoursPreviewItem = {
  title: string;
  value: string;
};